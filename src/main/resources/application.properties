spring.application.name=chat
server.address=0.0.0.0
server.port=3000

# Thymeleaf - disable template check since we use static HTML
spring.thymeleaf.check-template-location=false

# Logging - set to INFO for production (use DEBUG only when troubleshooting)
logging.level.root=INFO
logging.level.com.sts=INFO
